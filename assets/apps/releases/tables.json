{
  "tables": [
    {
      "name": "prod",
      "label": "Production",
      "icon": "public",
      "description": "The build the PRODUCTION site is serving right now. Fetched live from isan-web/build_info.json on every Refresh (mode: replace, so a refresh overwrites rather than accumulates). Seeded empty and never hand-edited — a build stamp is only ever what a real site returned.",
      "provenance": {
        "source": "isan-web build_info.json (Refresh action)"
      },
      "source": {
        "type": "httpAction"
      }
    },
    {
      "name": "dev",
      "label": "Dev",
      "icon": "construction",
      "description": "The build the DEV site is serving right now. Fetched live from isan-web-dev/build_info.json on every Refresh (mode: replace). Seeded empty for the same reason as prod.",
      "provenance": {
        "source": "isan-web-dev build_info.json (Refresh action)"
      },
      "source": {
        "type": "httpAction"
      }
    }
  ]
}
